[project]
name = "trusted-devices-gui"
version = "0.1.0"
description = "Veridevice Management Console"
readme = "README_GUI.md"
requires-python = ">=3.14"
dependencies = [
    "nicegui",
    "httpx",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "respx",
]
